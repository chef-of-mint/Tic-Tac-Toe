<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.loginregisterfirebase">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_start_page"
        android:label="Online Tic Tac Toe"
        android:roundIcon="@drawable/logo_start_page"
        android:supportsRtl="true"
        android:theme="@style/Theme.LoginRegisterFirebase">
        <activity android:name=".ui.PostBlogActivity"></activity>
        <activity
            android:name=".MainActivity2"
            android:label="@string/title_activity_main2"
            android:theme="@style/Theme.LoginRegisterFirebase.NoActionBar" />
        <activity
            android:name=".SplashScreen"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HomeActivity" />
        <activity android:name=".register_form" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>